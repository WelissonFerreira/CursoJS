/*let num = [5, 8, 2, 9, 3];
num.push(1);  // Adiciona o valor 1 ao final do array
num.sort();   // Organiza os valores do array em ordem alfabética (ou numérica, dependendo da situação)
console.log(num);  // Exibe o array após o push e o sort
console.log(`O vetor tem ${num.length} posições`);  // Exibe o tamanho do array
console.log(`O primeiro valor do vetor é ${num[0]}`);  // Exibe o primeiro valor do array
*/




let v = [5, 8, 2, 9, 3]

for (let i = 0; i < v.length; i++) {
    console.log(v[i])
}
